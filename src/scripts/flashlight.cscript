NEW_SCRIPT(flashlight)
	PARAMS_COUNT(1);

	vec3 *targetRot = &node->params[0].v3;

	glm_vec3_copy(node->parent->children[0]->rot, *targetRot);
	Vec3fApproach(node->rot, *targetRot, (0.1/delta));

END_SCRIPT(flashlight)